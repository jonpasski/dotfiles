defscrollback 5000
vbell off
source ${HOME}/.screenrc_local

screen -t "prevent root"
stuff "cd ~/src/prevent"
screen -t "prevent checkers"
stuff "cd ~/src/prevent/analysis/checkers/security/checkers"
screen -t "prevent packages"
stuff "cd ~/src/prevent/packages"
screen -t "ipython"
stuff "ipython"
screen -t "bash"
